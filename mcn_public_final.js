/* mcn_public_final.js — DÉSACTIVÉ STABLE v3.2
   Ancien patch neutralisé : il contenait des MutationObserver sur le texte
   qui entraient en boucle avec lang_fix.js sur les pages Visiter / À propos.
   Conservé uniquement parce qu'il est référencé dans les pages HTML.
*/
(function(){
  'use strict';
})();
